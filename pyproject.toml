[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "project-atlas"
version = "0.2.0"
description = "Adaptive Agentic Knowledge Runtime (Project Atlas)"
requires-python = ">=3.11"
authors = [{ name = "Project Atlas" }]
readme = "README.md"
license = { file = "LICENSE" }

dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
  "pyyaml>=6.0.1",
  "httpx>=0.27",
  "python-multipart>=0.0.12",
  "sqlalchemy>=2.0",
  "psycopg[binary]>=3.1",
  "qdrant-client>=1.9.0",
  "docling",
  "onnxruntime>=1.17",
  "PyMuPDF>=1.24.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "ruff>=0.6",
]

ui = [
  "streamlit>=1.31",
]

# Optional: lightweight HTML -> Markdown conversion for text/html ingestion
# when Docling is not installed.
html = [
  "markdownify>=0.13.1",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
  "integration: integration tests that hit external services (e.g., Docker Qdrant); skipped unless explicitly selected",
]
